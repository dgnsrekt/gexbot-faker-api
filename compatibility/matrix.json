[
  {
    "id": "classic-chain",
    "method": "GET",
    "path": "/{ticker}/classic/{category}",
    "status": "compatible",
    "summary": "Route, auth (Authorization Basic/Bearer header), gex_* path segment, and JSON body all match the live API."
  },
  {
    "id": "state-profile",
    "method": "GET",
    "path": "/{ticker}/state/{category}",
    "status": "compatible",
    "summary": "Route, header auth, and GEX vs Greek selection (gex_* vs gamma_zero/etc.) all match the live API."
  },
  {
    "id": "orderflow",
    "method": "GET",
    "path": "/{ticker}/orderflow/{category}",
    "status": "compatible",
    "summary": "Route, header auth, and observed live response fields match. (Upstream OpenAPI overstates the response; the live probe is the oracle.)"
  },
  {
    "id": "classic-maxchange",
    "method": "GET",
    "path": "/{ticker}/classic/{category}/maxchange",
    "status": "compatible",
    "summary": "Route, header auth, gex_* path, and top-level fields all match the live API."
  },
  {
    "id": "state-maxchange",
    "method": "GET",
    "path": "/{ticker}/state/{category}/maxchange",
    "status": "compatible",
    "summary": "Route, header auth, gex_* path, and top-level fields all match the live API."
  },
  {
    "id": "classic-majors",
    "method": "GET",
    "path": "/{ticker}/classic/{category}/majors",
    "status": "compatible",
    "summary": "Route, header auth, gex_* path, and top-level fields all match the live API."
  },
  {
    "id": "state-majors",
    "method": "GET",
    "path": "/{ticker}/state/{category}/majors",
    "status": "compatible",
    "summary": "Route, header auth, gex_* path, and top-level fields all match the live API."
  },
  {
    "id": "tickers",
    "method": "GET",
    "path": "/tickers",
    "status": "behavior-mismatch",
    "summary": "Shape matches, but the faker reports only loaded data while the live endpoint reports the supported universe."
  },
  {
    "id": "categories",
    "method": "GET",
    "path": "/{package}/categories",
    "status": "compatible",
    "summary": "Implemented: returns the package's category names (config.ValidCategories). Matches the live array response."
  },
  {
    "id": "quant-tickers",
    "method": "GET",
    "path": "/tickers/quant",
    "status": "compatible",
    "summary": "Implemented: returns quant-supported stocks and indexes (no futures), matching the live response fields."
  },
  {
    "id": "option-expiries",
    "method": "GET",
    "path": "/options/{ticker}/expiries",
    "status": "compatible",
    "summary": "Implemented: expiries over the loaded date + 90-day horizon; near-term dates grounded in the data min_dte/sec_min_dte, the rest from the options calendar."
  },
  {
    "id": "historical-download",
    "method": "GET",
    "path": "/hist/{ticker}/{package}/{category}/{date}",
    "status": "compatible",
    "summary": "Implemented: returns a {url} pointing at the faker /download route for that date/ticker/category (mirrors the live URL-indirection)."
  },
  {
    "id": "historical-eod-report",
    "method": "GET",
    "path": "/hist/eod/{ticker}",
    "status": "compatible",
    "summary": "Implemented: serves the latest EOD report zip for the ticker (the archive the daemon downloads)."
  },
  {
    "id": "futures-conversion",
    "method": "GET",
    "path": "/futures/conversion",
    "status": "compatible",
    "summary": "Implemented: front-month contract code from the loaded date + fixed per-pair basis rendered in additive/multiplicative/affine models. Required ticker/future validated with the live error bodies."
  }
]
