---
title: GEX Faker knowledge base
description: Mock server that replays historical GexBot options/GEX market data over REST and WebSocket, with a web UI (Studio) and an agent CLI (gexfakercli).
---

import GuideCard from '../../components/GuideCard.astro';

The **GEX Faker** replays historical GexBot options/GEX market data over REST and
WebSocket — a drop-in mock for its primary data surface, with a web UI (Studio)
and an agent CLI (`gexfakercli`). New here? Start with the
[quick start](quick-start/). Building an agent? Point it at
[`llms.txt`](llms.txt) or `knowledge/index.md`.

{/* Card hrefs are BASE_URL + "/slug/". BASE_URL has no trailing slash in either
    build ("/guides", "/gexbot-faker-api"), so the leading slash is in the literal.
    Glyphs reuse the Studio SPA's nav icon language. */}

## Use it

<div class="guide-grid">
  <GuideCard glyph="⬡" title="Overview" description="What the faker is, what it replays, and the pieces at a glance." href={import.meta.env.BASE_URL + '/overview/'} />
  <GuideCard glyph="▸" title="Quick start" description="From clone to a live stream in the Studio — the golden path." href={import.meta.env.BASE_URL + '/quick-start/'} />
  <GuideCard glyph="▣" title="Studio" description="The seven screens of the web UI and what each one does." href={import.meta.env.BASE_URL + '/studio/'} />
  <GuideCard glyph="↓" title="Download data" description="The two ways data arrives (EOD archive vs /hist) and the API key." href={import.meta.env.BASE_URL + '/download-data/'} />
  <GuideCard glyph="⇡" title="Materialize & load" description="The archived → ready → loaded lifecycle (the #1 confusion)." href={import.meta.env.BASE_URL + '/materialize-load/'} />
  <GuideCard glyph="▦" title="Docker & observability" description="The compose stack, Prometheus/Loki, and the Monitoring screen." href={import.meta.env.BASE_URL + '/docker-observability/'} />
</div>

## Build with it

<div class="guide-grid">
  <GuideCard glyph="⇆" title="Point a client" description="Base URL, header auth, and per-key playback." href={import.meta.env.BASE_URL + '/point-a-client/'} />
  <GuideCard glyph="❯" title="gexfakercli" description="The JSON-first agent CLI — setup, describe, data pulls, cursor." href={import.meta.env.BASE_URL + '/gexfakercli/'} />
  <GuideCard glyph="⧉" title="REST API" description="The endpoint surface, Swagger UI, and the OpenAPI spec." href={import.meta.env.BASE_URL + '/rest-api/'} />
  <GuideCard glyph="≈" title="WebSocket streaming" description="The five hubs, /negotiate, group naming, and the frame format." href={import.meta.env.BASE_URL + '/websockets/'} />
  <GuideCard glyph="⚙" title="Configuration" description="The environment variables that configure the faker." href={import.meta.env.BASE_URL + '/configuration/'} />
  <GuideCard glyph="⟳" title="Daemon" description="Scheduled downloads, coverage alerts, and ntfy push." href={import.meta.env.BASE_URL + '/daemon/'} />
</div>

## When something is wrong

<div class="guide-grid">
  <GuideCard glyph="⚠" title="Troubleshooting" description="No data, archived vs ready, auth 400s, exhausted cursors." href={import.meta.env.BASE_URL + '/troubleshooting/'} />
</div>
